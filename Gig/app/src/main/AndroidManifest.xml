<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lucas.gig">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activitys.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activitys.TelaInicialActivity" />
        <activity android:name=".activitys.CadastrarActivity" />
        <activity android:name=".activitys.PerfilActivity" />
        <activity android:name=".activitys.CriarEventoActivity" />
        <activity android:name=".activitys.CriarContratoActivity" />
        <activity android:name=".activitys.CriarContrato2Activity"></activity>
    </application>

</manifest>